//Given a string, if the first or last chars are 'x', return the string without those 'x' chars, and otherwise return the string unchanged.

//withoutX("xHix") → "Hi"
//withoutX("xHi") → "Hi"
//withoutX("Hxix") → "Hxi"

public String withoutX(String str) {
  if (str.length() == 0) return str;  //for null
  if (str.length() ==1) {   //to prevent index out of bounds error when trying to get end x with one char strings
    if (str.charAt(0) == 'x') return "";  
    return str;
  }
