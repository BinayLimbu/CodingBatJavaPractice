//Given 2 ints, a and b, return true if one if them is 10 or if their sum is 10.

//makes10(9, 10) → true
//makes10(9, 9) → false
//makes10(1, 9) → true

public boolean makes10(int a, int b) {
if ((a + b == 10) || (a == 10) || (b == 10)) {  //only one pair of outer brackets actually necessary
  }
  return true;
  }
else return false;
}

//entire condition can actually be shortened to one line:
//return (a == 10 || b == 10 || a+b == 10);
